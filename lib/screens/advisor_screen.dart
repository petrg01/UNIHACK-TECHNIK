import 'package:flutter/material.dart';

class AdvisorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Advisor Screen Content", style: TextStyle(fontSize: 20)),
    );
  }
}
